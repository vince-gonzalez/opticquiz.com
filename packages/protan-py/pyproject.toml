[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "protan"
version = "0.1.0"
description = "Simulate protanopia (red-blind) color vision — a typed shortcut over cvdsim / the OpticQuiz engine (Machado, Oliveira & Fernandes 2009)."
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [{ name = "Vince Gonzalez" }]
keywords = ["protan", "protan", "colorblind", "color-blindness", "cvd", "simulate", "color-vision", "accessibility", "a11y"]
dependencies = ["cvdsim>=0.1.0"]

[project.optional-dependencies]
image = ["cvdsim[image]>=0.1.0"]

[project.urls]
Homepage = "https://opticquiz.com/"
Source = "https://github.com/vince-gonzalez/opticquiz.com"

[tool.setuptools]
packages = ["protan"]
