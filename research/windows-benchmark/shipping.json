{
  "_comment": "What is DECLARED to ship, per deficiency and per surface. verify_shipped.py checks the source files against this, so the decision is explicit and reviewable rather than inferred from which transform files happen to exist on disk.",
  "linear_surfaces": {
    "protan": {
      "transform": "opticquiz-v3-protan",
      "reason": "v3 SHIPPED. Net +47 on 10 real palettes vs v2 +30 and daltonize +31, at LOWER distortion (10.51 vs 13.80 / 14.61). Held out on 4 unseen palettes under 3 unseen simulators: +25 vs v2's +20. Closes the protanopia gap that v1 and v2 both lost to daltonize."
    },
    "deutan": {
      "transform": "opticquiz-v3-deutan",
      "reason": "v3 SHIPPED. Net +65 vs v2 +31 and daltonize +39, at lower distortion (9.51). Breaks 11 pairs where v2 breaks 53. Held out: +22 vs v2's +5."
    },
    "tritan": {
      "transform": "opticquiz-v2-tritan",
      "reason": "STAYS ON v2. v3 tritan OVERFIT - fit net +15, held-out +1 against v2's +7 - and was refused. v2 is net +17 on real palettes (with the dE>=20 distinctness filter), a genuine improvement on v1's -1."
    }
  },
  "desktop_app": {
    "transform": {
      "protan": "opticquiz-protan",
      "deutan": "opticquiz-desktop-deutan",
      "tritan": "opticquiz-desktop-tritan"
    },
    "reason": "MagSetFullscreenColorEffect applies its matrix to GAMMA-ENCODED sRGB, not the linear light every browser surface uses, so the browser matrices are not transferable. deutan and tritan are now derived FOR sRGB space (held out: +18 and +4, against +3 and -4 for the browser matrices run here). protan stays on the original - its sRGB candidate tied on net (+16 vs +17) and a tie is not a win."
  }
}